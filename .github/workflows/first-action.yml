name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: |
          echo 'Hello world'
          echo 'Hello world2'
      - name: Print Goodbye
        run: echo 'Done - bye!'
